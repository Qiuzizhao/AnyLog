const prefix = 'anylog.local';

export const localKeys = {
  checkinProjects: `${prefix}.checkinProjects`,
  checkinRecords: `${prefix}.checkinRecords`,
  settings: `${prefix}.settings`,
  meta: `${prefix}.syncMeta`,
};
